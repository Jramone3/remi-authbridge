# Instalación de dependencias

Este proyecto no incluye `node_modules` en el repositorio por razones patrimoniales y de peso.

Para reconstruir las dependencias, ejecute:

```bash
npm install

o

yarn install
